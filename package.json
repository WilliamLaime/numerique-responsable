{
  "name": "numerique-responsable",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Extension Chrome — audit accessibilité (RGAA) et écoconception (RGESN).",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
